
Para instalar bundle en mac BK, usar :
http://stackoverflow.com/questions/21095098/why-wont-bundler-install-json-gem


1. gem install bundler -v '= 1.5.1'

2. gem install json

3. gem install rake



gem install bundler -v '= 1.3.5'


gem install json -v '2.0.3'
gem install json -v '1.8.2'

________________________________
PASOS COMPU CODEA

rake db:create
$ rake db:migrate
$ rake db:seed


sqlite3 db/tasks.sqlite3
SELECT * FROM users;
SELECT * FROM decks;
SELECT * FROM rounds;
SELECT * FROM questions;
SELECT * FROM answers;
SELECT * FROM round_questions;


users = User.all
decks = Deck.all
rounds = Round.all
questions = Question.all
answers = Answer.all
round_questions = RoundQuestion.all


Question.where(question: '¿Cuál es la rama mayoritaria del Islam?').take
The SQL equivalent of the above is:

SELECT * FROM clients WHERE (clients.first_name = '¿Cuál es la rama mayoritaria del Islam?') LIMIT 1


RoundQuestion.where(score: 0).take

RoundQuestion.where(score: 0).count

